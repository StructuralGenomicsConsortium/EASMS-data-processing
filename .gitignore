# Data folders (real datasets, kept locally only)
ProcessedData*/
RawData*/
MasterLists/
MultiBatch_*/
MultiBatchResults/

# Raw ASMS data exports (contain protein names / compound data)
asms_sgcto_*.csv

# Provider list (real version contains private company info; sample is tracked)
Providers.csv

# Local-only scratch / unused scripts
src/tempfiles/

# Multi-batch analysis / exploration scripts: track code only, never data/output
multibatchcodes/*
!multibatchcodes/*.py

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
