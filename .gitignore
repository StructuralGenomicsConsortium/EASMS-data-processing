# Data folders (real datasets, kept locally only)
ProcessedData*/
RawData/
MasterLists/

# Provider list (real version contains private company info; sample is tracked)
# Providers.csv

# Local config for the Makefile (real values; .env.example is tracked)
.env

# Local-only scratch / unused scripts
src/tempfiles/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
.claude/
CLAUDE.*
easms-curation/